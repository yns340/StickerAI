import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("App settings and preferences.")
                .navigationTitle("Settings")
        }
    }
}
