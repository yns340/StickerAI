import SwiftUI

struct LibraryView: View {
    var body: some View {
        NavigationStack {
            Text("Your saved images will appear here.")
                .navigationTitle("Library")
        }
    }
}
